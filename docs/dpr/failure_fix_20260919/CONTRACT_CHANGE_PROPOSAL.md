# Separate proposal — UNAPPROVED / NOT_IMPLEMENTED

Current admission remains **BLOCKED**. This document is not an instruction to
relax a gate and does not authorize training. No item below is implemented.

1. **Backend candidate sensitivity:** if a new exact-server trace demonstrates
   continuous local/target/P3/P4/P5/encoder/score paths passing original tolerance,
   changed actual indices, one correctly scoped fixed replay passing, matching
   file/model/input identity and finite outputs, a future explicit decision could
   align the backend candidate exception with native_fuse. Currently backend
   requires raw allclose; original CPU backend failure is still unexplained.
   Continuous or replay failure, identity mismatch, and nonfinite data would
   remain disqualifying even under that proposed alternative.
2. **Independent backward B:** original candidate failed-name subset and all
   added DPR raw state/gradient requirements fail on server evidence and AMP
   fails locally. A possible separate research contract could require new-SHA A,
   independent same-run/delta analytic adjoints and frozen local replay plus
   prespecified replicated trajectory evidence instead of raw independent
   gradient equality. This changes the current contract and is not justified as
   an equivalent guarantee by the present bounded observations. Neither parent
   failure counts nor checkpoint A alone is sufficient. Do not drop the subset,
   accept counts instead, quantize state, or waive the four raw gradients now.
3. **Whole-network half:** original P5/encoder/target failures remain real.
   Any dtype policy change, selective FP32 path or tolerance adjustment changes
   the requested experiment and requires a separate specification and explicit
   authorization. The current evidence supports no automatically acceptable
   replacement threshold. Parent/ordinary-conv background and small exceeded
   fractions cannot pass the original gate.

The immediate next step is the finite exact-server diagnostic. Preserve its
original raw failures and attribution evidence. If a concrete implementation
defect is established, fix it under the existing protocol and validate again.
Otherwise obtain an explicit contract decision before proposing a new eligible
full-preflight run. Long-term trajectory equivalence is **NOT_CLAIMED**.
